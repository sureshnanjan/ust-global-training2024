using Gopinath.training;
using System;
class MyClass{
static void Main(){
	// Expected class, delegate, enum, interface, or struct
	Console.WriteLine("Automation Training_Assignment1");
	TrainingDashboard.publish();
	TrainingDashboard.generateReport();
}
}